spring.application.name=UberProject-AuthService

spring.datasource.url=jdbc:mysql://localhost:3306/uber_db_local?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=112233

# Hibernate fully manages schema (creates tables from entities)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true          # Makes logs readable

# IMPORTANT: Disable Flyway for now
spring.flyway.enabled=false

# Optional but helpful
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

server.port=7475
jwt.expiry=3600
cookie.expiry=3600
jwt.secret=algocampalgocampalgocampalgocampalgocampalgocampalgocampalgocampalgocampalgocamp
